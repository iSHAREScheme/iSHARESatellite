services:
  edge-acme:
    image: caddy:2.9.1-alpine
    container_name: edge-acme
    network_mode: host
    restart: always
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - <ACME_STATE_DIR>/data:/data
      - <ACME_STATE_DIR>/config:/config
